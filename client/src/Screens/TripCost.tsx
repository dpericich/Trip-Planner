const TripCost = () => {
    return (
        <div className="">
            I will include all the costs for the Trip
            {/* Maybe each item has an optional cost input field and this will pull in that data to show running costs */}
            {/* Could also use graphing to display categorical breakdown (bar and pie chart) */}
        </div>
    )
};

export default TripCost;