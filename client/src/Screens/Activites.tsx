const ActivitiesScreen = () => {
    return (
        <div className="">
            {/* This will be a table that allows users to add in activities they want */}
            {/* It will allow draft records for the user to enter potential activities */}
            {/* If the user wants to add it to the schedule the times will be validated against the current schedule */}
        </div>
    )
};

export default ActivitiesScreen;