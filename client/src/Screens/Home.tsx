const HomeScreen = () => {
    return (
        <div className="">
            {/* This will prompt the user to create a travel plan if none exists */}
            {/* Otherwise it will show existing trips and allow the user to work on them */}
        </div>
    )
};

export default HomeScreen;